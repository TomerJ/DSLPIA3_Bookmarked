FUNCTION validateAccessCode (code)
BEGIN
    VAR results = QUERY select from access_codes where code is inputted code
    IF results.length > 0 THEN
        
    ENDIF 
END



FUNCTION signup (parameters)
BEGIN
    VALIDATE 
END